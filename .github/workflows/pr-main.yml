name: On PR

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  id-token: write
  actions: write
  contents: read
  packages: write
  checks: write
  pull-requests: write

jobs:
  performance-benchmark:
    uses: ./.github/workflows/perf-benchmark.yml
    if: contains(github.event.pull_request.labels.*.name, 'dummy-label')
    secrets: inherit
    with:
      docker-image: ghcr.io/tenstorrent/tt-xla-slim:nightly-latest
      project: tt-xla
